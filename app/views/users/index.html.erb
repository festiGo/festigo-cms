<h1><%= t('.title') %></h1>

<ul class="toolbar inline">
  <!-- toolbar with large action buttons -->
  <% if can? :create, User %>
    <li><%= link_to t('.add_user'), new_user_path, :class => "btn btn-success" %></li>
  <% end %>
</ul>

<%= render 'users/listing' %>


<%= link_to t('common.back'), admin_index_path %>